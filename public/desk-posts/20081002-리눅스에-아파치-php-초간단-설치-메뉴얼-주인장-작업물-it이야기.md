---
date: "2008-10-02"
titleKo: 리눅스에 아파치, PHP 초간단 설치 메뉴얼 주인장 작업물 / IT이야기
titleEn: Super Simple Apache, PHP Installation Manual on Linux
category: it
tags:
  - 주인장 작업물
images: []
sourceCategoryNo: "16"
sourceCategory: 주인장 작업물
externalUrl: https://blog.naver.com/fstory97/70035550579
---

<!-- ko -->
**1. 제목 : 리눅스에 아파치, PHP 초간단 설치 메뉴얼**
* 주의할점 : 초간단하지 않을지도 모릅니다. 비교적 초간단이죠.

**2. 목적**
회사에 업무 용으로 APM과 PHP를 설치하게 되었서 메뉴얼을 작성해보기로 했습니다.

**3. 작성자 및 도움주신이
**작성자 숲속얘기 [[fstory97@naver.com](mailto:fstory97@naver.com) / [http://blog.naver.com/fstory97](http://blog.naver.com/fstory97)]
도움주신이 하얀별님 [[wono77@naver.com](mailto:wono77@naver.com) / [http://blog.naver.com/wono77](http://blog.naver.com/wono77)]
**초안은 하얀별님이 작성해주셨습니다. 질문은 되도록 하얀별님한테....**

**4. 작성자 잡담**
윈도우같이 인스톨 실드같은게 있으면 얼마나 좋아 ㅡㅡ
버젼과 OS마다 특성을 타는 수가 있다. 그래서 리눅스유저들은 "설치"란 용어 대신 "배포"란 용어를 쓰나 보다. 귀찮으니까 알아서 찾아서 깔아보라는거지.. 서로 각 종 프로그램들이 의존성을 가진것을 보면 뭐 하나 쓸려고 그래도 참 산너머 산이다. (리눅스 유저들한테 욕먹을 수 있으니 비방은 여기까지.. 무료라니 참아야지.)

**5. 설치할 프로그램들**
* 아파치 : httpd-2.2.9.tar.gz 소스
* PHP: 5.2.6 소스(stable/ alpha는 5.3)
* Mysql: 5.0.67 소스(디지타이징에서는 5.1.7/ 현재 최신버젼은 6.0)

**6. 설치해본 환경**
공짜 64bit CentOS (버젼 확인이 귀찮으니 패스합니다.)

**7. 설치실험 일**
2008년 10월 2일

**8. 문서내의 색깔**
빨간색은 사용자가 직접 칠 부분입니다. 빨간색으로 작성된 부분만 Copy & Paste해서 돌려보면 되겠습니다.
****
**9. 프로그램 다운로드**
터미널 창에서 아래와 같이 치면 됩니다. (Copy & Paste 해서 실행해도 관계 없습니다. 링크가 살아있다면..)**

10. 압축 해제

11. 아파치 설치

| **1. 배치 셸 만들고 실행하기** 압축이 팔린 아파치 폴더로 가서 아래와 같은 설치 셸(실행 텍스트 파일)을 만듭니다. cd httpd-2.2.9/ vi config.nice txt파일에 실행권한을 줘서 셸스크립트로 바꿉니다. chmod 755 config.nice 실행합니다. ./config.nice **** **2. 바이너리 코드 만들기** make && make install **** **3. 설치 폴더에 계정 권한 설정 하기** 설치된 아파치 폴더로 이동하여 폴더에 계정의 권한을 설정합니다. 자신의 그룹과 이름을 입력해주면 됩니다. 설치 폴더는 셸에 --perfix에 적혀 있는 Path chown -R [그룹명]:[계정이름] [설치 Path] ex) chown -R fstory:fstory /home/fstory/apahce/ **4. ./conf/httpd.conf 수정** 설치 폴더에 ./conf/httpd.conf 파일을 vi같은 에디터로 열고 수정합니다. *** 초록색은 설명이므로 붙여 넣치 마세요. **** 1) 환경 설정 ** 편집기에서 Find 기능을 써서 **Listen 80**을 찾고, 래에 추가 옵션(빨간색)을 붙여 넣습니다. 1.x대버젼에는 Port 80 입니다. ** 2) 로그 설정 ** 에러가 생겼을때 로그를 기본폴더가 아닌 다른곳(/home/logs/https-digit)에 특정 형태(myformat)로쌓기 위한 설정 ** ErrorLog "logs/error_log"** 를 찾아서 아래와 같이 변경 해주세요. 그리고, 미리 로그가 쌓일 /home/logs/https-digit/ 폴더를 생성해 둔다. **3) 언어 타입 변경** ** 4) 서비스 기본 소스 위치 변경** DocumentRoot를 찾아서 원하는 디렉토리로 변경 (index.html/ index.php 위치할곳) ** 5) 접근 허가 설정 (2.2.x 버젼 이후부터)** <Directory />...</Directory> 를 찾아 아래와 같이 변경한다. **5. 에러 로그를 정기적으로 gz으로 압축 백업하고 지우는 쉘스크립트 작성** 실제 서비스가 목적이라면 로그가 엄청나게 쌓이므로 주기적으로 지워줘야 할수도 있다. ** 1) rotate_log.sh 셸크립트 작성** ** 2) chmod +x ./rotate_log.sh 하여 실행 권한은 부여 **** 3) 셸을 root의 cronttab에 적용** crontab -e **6. 실행해보자** apache 설치 폴더에 가셔서.. ./bin/apachectl start 로그 폴더가 생성이 안되었다면 에러가 나오는데 해당 폴더를 생성하시고 다시 실행해보세요. **7. 정지 시켜 보자 **./bin/apachectl stop **8. 서버 부팅시 자동 Apache를 자동 실행 시켜보자.** S는 서버 시작시 자동 실행되는것을 의미하며, K는 서버 종료시 자동시작되는 것을 의미합니다. 뒤의 숫자는 우선순위입니다.. 소문자 s나 다른 알파벳으로는 실행이 되지 않습니다. 아래의 명령어를 Apache 설치 bin 폴더에 가서 실행하시면 됩니다. ******** | #!/bin/sh # # Created by configure "./configure" \ "--prefix=/homel/fstory/apache" \ # 설치 path "--enable-so" \ "--enable-rule=SHARED_CORE" \ "--enable-mods-shared=all" \ "--enable-modules=most" \ "--enable-suexec" \ "--with-suexec-caller=bin" \ "--enable-cache" \ "--enable-ext-filter" \ "--with-z=/usr/include" \ "--enable-dav" \ "$@" | Listen 80 #서비스 포트 번호 기본값은 80입니다. Timeout 120 #세션이 응답없을 때 종료하는 시간입니다. 저희 프로젝트는 좀 깁니다. KeepAlive off # 유저가 없어도 항상 살아 있으라는건지 전 몰라요. 누가 알려주.. User fstory # 유저명 Group myteam # 그룹명 DirectoryIndex index.html index.htm index.php # 서비스 시작파일명 | ErrorLog /home/fstory/logs/https-digit/error #log 위치 LogFormat "%a - %u %t \"%r\" %s %b - -" myformat SetEnvIf Request_URI "^\/bin\/" dontlog SetEnvIf Request_URI "\.(gif|jpg|png|bmp|swf)" dontlog SetEnvIf Request_URI "\.(css|js)" dontlog CustomLog /home/fstory/logs/https-digit/access myformat env=!dontlog | AddDefaultCharset utf-8 # 해당 내용이 없으면 기본 값은 euc_kr로 동작 | DocumentRoot "/home/www_src/" | <Directory /aaa> Order allow,deny allow from all </Directory> | #!/bin/sh LOG_DIR=/home/logs/https-digit DATE=`/bin/date +%y%m%d` YDATE=`/bin/date --date "yesterday" +%Y%m%d` # cronlolog를 사용하지 않는 경우 /bin/cp $LOG_DIR/access $LOG_DIR/access.$DATE /bin/cat /dev/null > $LOG_DIR/access /bin/gzip -f $LOG_DIR/access.$DATE # cronolog를 사용하는 경우 #/bin/mv $LOG_DIR/access.$YDATE $LOG_DIR/access.$DATE #/bin/gzip $LOG_DIR/access.$DATE #cat /dev/null > $LOG_DIR/error find $LOG_DIR/ -mtime +15 -name "access*" -exec rm {} \; | 01 00 * * * /home/fstory/shell/rotate_log.sh #shell 스크립트 위치를 지정 | cp apachectl /etc/rc.d/init.d/httpd ln -s /etc/rc.d/init.d/httpd /etc/rc.d/rc3.d/S89httpd ln -s /etc/rc.d/init.d/httpd /etc/rc.d/rc3.d/K89httpd |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| #!/bin/sh # # Created by configure "./configure" \ "--prefix=/homel/fstory/apache" \ # 설치 path "--enable-so" \ "--enable-rule=SHARED_CORE" \ "--enable-mods-shared=all" \ "--enable-modules=most" \ "--enable-suexec" \ "--with-suexec-caller=bin" \ "--enable-cache" \ "--enable-ext-filter" \ "--with-z=/usr/include" \ "--enable-dav" \ "$@" |  |  |  |  |  |  |  |  |  |
| Listen 80 #서비스 포트 번호 기본값은 80입니다. Timeout 120 #세션이 응답없을 때 종료하는 시간입니다. 저희 프로젝트는 좀 깁니다. KeepAlive off # 유저가 없어도 항상 살아 있으라는건지 전 몰라요. 누가 알려주.. User fstory # 유저명 Group myteam # 그룹명 DirectoryIndex index.html index.htm index.php # 서비스 시작파일명 |  |  |  |  |  |  |  |  |  |
| ErrorLog /home/fstory/logs/https-digit/error #log 위치 LogFormat "%a - %u %t \"%r\" %s %b - -" myformat SetEnvIf Request_URI "^\/bin\/" dontlog SetEnvIf Request_URI "\.(gif|jpg|png|bmp|swf)" dontlog SetEnvIf Request_URI "\.(css|js)" dontlog CustomLog /home/fstory/logs/https-digit/access myformat env=!dontlog |  |  |  |  |  |  |  |  |  |
| AddDefaultCharset utf-8 # 해당 내용이 없으면 기본 값은 euc_kr로 동작 |  |  |  |  |  |  |  |  |  |
| DocumentRoot "/home/www_src/" |  |  |  |  |  |  |  |  |  |
| <Directory /aaa> Order allow,deny allow from all </Directory> |  |  |  |  |  |  |  |  |  |
| #!/bin/sh LOG_DIR=/home/logs/https-digit DATE=`/bin/date +%y%m%d` YDATE=`/bin/date --date "yesterday" +%Y%m%d` # cronlolog를 사용하지 않는 경우 /bin/cp $LOG_DIR/access $LOG_DIR/access.$DATE /bin/cat /dev/null > $LOG_DIR/access /bin/gzip -f $LOG_DIR/access.$DATE # cronolog를 사용하는 경우 #/bin/mv $LOG_DIR/access.$YDATE $LOG_DIR/access.$DATE #/bin/gzip $LOG_DIR/access.$DATE #cat /dev/null > $LOG_DIR/error find $LOG_DIR/ -mtime +15 -name "access*" -exec rm {} \; |  |  |  |  |  |  |  |  |  |
| 01 00 * * * /home/fstory/shell/rotate_log.sh #shell 스크립트 위치를 지정 |  |  |  |  |  |  |  |  |  |
| cp apachectl /etc/rc.d/init.d/httpd ln -s /etc/rc.d/init.d/httpd /etc/rc.d/rc3.d/S89httpd ln -s /etc/rc.d/init.d/httpd /etc/rc.d/rc3.d/K89httpd |  |  |  |  |  |  |  |  |  |

**

**12. MySql 설치 **: 5.0.67 소스판을 기준으로 합니다.

| **** **1. 설치 옵션 스크립트 작성 ** vi config.nice (설치하시고 싶은 디렉토리로 작성하세요.) **2. 실행 권한 부여** chmod +x config.nice **3. 실행** ./config.nice **4. 설치 : root 권한으로 설치하세요.** make&make install **5. 환경설정** 설치된 mysql 디렉토리로 이동합니다. vi my.cnf **6. 임시 폴더 생성** mkdir tmp **7. db 설치 ** 반드시 root 권한이어야 합니다. ../bin/mysql_install_db 실행후, ./mysql/var 디렉토리와 안에 내용들이 생성되었는지 확인합니다. **8. 폴더에 계정 권한 부여 ** mysql안의 파일들의 모든 권한을 해당 사용 아이디의 권한으로 바꿔줍니다. chown -R [그룹명]:[계정이름] [설치 Path] **8. mysql을 시작합니다.** ./bin/mysqld_safe & 혹시 시작이 안되면, /etc/my.cnf가 이미 존재하고 있는 경우일수도 있습니다. 새로 만든 my.cnf로 덮어쓰고 /var/lib/mysql 폴더의 생성된 db를 지우고, mysql_install_db 부터 다시해봅니다. **9. mysql 을 종료해봅니다.** ./bin/mysqladmin shutdown **10. 다시 mysql 을 시작하고, root 패스워드를 설정합니다. ** ./bin/mysqladmin -uroot password '[유저패스워드]' **11. 접속을 해봅시다.** . ./bin/mysql -uroot -p패스워드 **12. 보안을 위한 설정 (mysql 접속후)** use mysql delete from user where user=' '; flush privileges; - 유저 비번 변경 - update user set password=password('root패스워드') where user='root'; **13. 다른 서버의 원격 접속 설정 (필요 없는 경우 Skip하세요.)** use mysql insert into user (Host,User,Password) VALUES ('원격접속할ip','계정명',password('패스워드')); **14. mysql 자동 시작하기 설정** 설치판은 ./mysql/share/mysql 에 mysql.server가 위치. 컴파일한 경우는 mysql.server가 자동으로 셋팅이 되어 있음 vi mysql.server cp mysql.server /etc/rc.d/init.d/ ln -s /etc/rc.d/init.d/mysql.server /etc/rc.d/rc3.d/S98mysql******** | ./configure --prefix=/home/fstory/mysql \ #설치 path --with-mysqld-user=fstory \ #계정명 --without-debug \ --with-extra-charsets=euckr --with-charset=utf8 \ --with-unix-socket-path=/home/mysql.sock \ --localstatedir=/home/mysql/var \ --sysconfdir=/home/mysql \ --with-tcp-port=3306 \ #mysql 포트번호 --with-readline \ --with-partition \ --with-big-tables \ | [mysql.server] port=3306 socket = /home/fstory/mysql/tmp/mysql.sock #설치 path 하고 맞출것 datadir = /home/fstory/mysql/var #설치 path 하고 맞출것 [mysqld] port=3306 socket = /home/fstory/mysql/tmp/mysql.sock #설치 path 하고 맞출것 datadir = /home/fstory/mysql/var #설치 path 하고 맞출것 set-variable = max_connect_errors=99999999 set-variable = flush_time=7200 set-variable = max_connection=256 set-variable = table_cache=2000 set-variable = key_buffer=196M set-variable = sort_buffer=4M set-variable = record_buffer=32M set-variable = wait_timeout=20 set-variable = thread_cache_size=60 set-variable = read_buffer_size=32M | basedir=/home/fstory/mysql #설치 폴더에 맞게 해주세요. datadir=/home/fstoryl/mysql/data # The following variables are only set for letting mysql.server find things.# Set some defaults pid_file=/home/fstory/mysql/tmp/mysql.sock |
| --- | --- | --- | --- |
| ./configure --prefix=/home/fstory/mysql \ #설치 path --with-mysqld-user=fstory \ #계정명 --without-debug \ --with-extra-charsets=euckr --with-charset=utf8 \ --with-unix-socket-path=/home/mysql.sock \ --localstatedir=/home/mysql/var \ --sysconfdir=/home/mysql \ --with-tcp-port=3306 \ #mysql 포트번호 --with-readline \ --with-partition \ --with-big-tables \ |  |  |  |
| [mysql.server] port=3306 socket = /home/fstory/mysql/tmp/mysql.sock #설치 path 하고 맞출것 datadir = /home/fstory/mysql/var #설치 path 하고 맞출것 [mysqld] port=3306 socket = /home/fstory/mysql/tmp/mysql.sock #설치 path 하고 맞출것 datadir = /home/fstory/mysql/var #설치 path 하고 맞출것 set-variable = max_connect_errors=99999999 set-variable = flush_time=7200 set-variable = max_connection=256 set-variable = table_cache=2000 set-variable = key_buffer=196M set-variable = sort_buffer=4M set-variable = record_buffer=32M set-variable = wait_timeout=20 set-variable = thread_cache_size=60 set-variable = read_buffer_size=32M |  |  |  |
| basedir=/home/fstory/mysql #설치 폴더에 맞게 해주세요. datadir=/home/fstoryl/mysql/data # The following variables are only set for letting mysql.server find things.# Set some defaults pid_file=/home/fstory/mysql/tmp/mysql.sock |  |  |  |

**13. php 설치**

| **1. 서브 라이브러리 설치** **1) zlib설치.... (root 여야만 합니다.) **./configure --prefix=/home/fstory/zlib make && make install **2) libiconv 설치...(IConv설치:euckr->utf8의 변환을 위해서..) **./configure --prefix=/home/fstoryl/iconv make && make install **3) libxml2 설치... ** (64비트 CentOS 에서 설치를 성공하지 못했습니다. ㅜㅜ.) ./configure --prefix=/home/fstory/libxml \ --with-zlib=/home/fstory/zlib \ --with-iconv=/home/fstory//iconv make && make install **4) libxslt 설치 **(libxml이 제대로 붙지 않았다면 이것도 의미 없다. 설치 하지 말자.) ./configure --prefix=/home/fstory/libxslt \ --with-libxml-prefix=/home/fstory/libxml \ --with-libxml-include-prefix=/home/fstory/libxml/include \ --with-libxml-libs-prefix=/home/fstory/libxml/lib make && make install **7) gd 설치하기** (썸네일 생성등에 사용) ./configure --prefix=/home/fstory/gd \ --with-gif --with-png --with-jpeg make make install **2. PHP 설치** ** 1) 설치 옵션 파일 만들기및 실행 ** 각 프로그램들의 Path를 맞춰주세요. 설치 안된게 있다면 빼버리시길 바랍니다. ex) iconv가 설치가 안되었다면, --with-iconv 줄을 날려주세요. vi config.nice chmod +x config.nice ./config.nice **2) 예외 처리** **3) 설치** make & make install **4) 구동 테스트 및 확인** 아파를 재시작하고, phpinfo()로 잘 설치되었나 확인해봅니다... 앞에서 아파치 구동테스트를 위해 AddType에 주석을 달거나, LoadModule libphp5.so 부분에 주석이 달렸다면 주석을 풀고 재시작하세요~ AddType application/x-httpd-php .php4 .php3 .phtml .php .inc .htm .html .naver .nhn AddType application/x-httpd-php-source .phps LoadModule php5_module modules/libphp5.so 설정변경 할건 대충 아래와 같습니다만. .(그냥 이전의 php.ini를 빼와서 aphche conf디렉토리에 넣으면 됩니다. 우리는 아파치,php 환경설정을 apache conf 디렉토리에 넣도록 합시다~ display_errors = On include_path = ".:/home/fstory/php_includes/common/:/home/fstory/php_includes/" mysql.socket의 절대 경로명 써줌. mysql.default_socket = /fstory/mysql/tmp/mysql.sock php_info()에서 아래사항을 확인합니다. mysql, mysqli 로딩되었는가? gd가 로딩되었는가? 특히 jpg가 포함되었는가? iconv확인 첨부된 php.ini 파일을 apache의 conf와 같은 폴더에 넣자. | # # Created by configure './configure' \ '--prefix=/home/fstory/php' \ '--with-config-file-path=/home/fstory/apache/conf' \ '--with-mysql=/home/fstory/mysql' \ '--with-mysqli=/home/fstory/mysql/bin/mysql_config' \ '--with-gd-dir=/home/fstory/gd' \ '--with-jpeg-dir=/usr/lib' \ '--with-apxs2=/home/fstory/apache/bin/apxs' \ '--enable-track-vars' \ '--with-jpeg' \ '--with-iconv' \ '--enable-ftp' \ '--enable-exif' \ '--enable-sockets' \ '--with-xml' \ '--with-gd' \ '--disable-cgi' \ '--with-gif' \ '--with-png' \ '--disable-posix-threads' \ "$@" | * 이때 **configure: error: libjpeg.(a|so) not found. **란 오류 메시지가 뜬다면 peg관련해서 하나 더 깔아야 한다. wget [http://freshmeat.net/redir/libjpeg/5665/url_tgz/jpegsrc.v6b.tar.gz](http://freshmeat.net/redir/libjpeg/5665/url_tgz/jpegsrc.v6b.tar.gz) gunzip jpegsrc.v6b.tar.gztar xvf jpegsrc.v6b.tarcd jpeg-6b ./configure make make install (이때 마지막에 에러뜸, 무시해도 좋다. root로 해도 마찬가지임) make install-lib make install-headers. * 다시 php 디렉토리로 와서 ./config.nice그래도 또 이번엔 libpng 가 없다고 나온다면.. ㅜㅜ (아~ 리눅스가 싫다.) wget [http://ftp.superuser.co.kr/pub/etc/libpng-1.2.5.tar.gz](http://ftp.superuser.co.kr/pub/etc/libpng-1.2.5.tar.gz) tar xvfz libpng-1.2.5.tar.gz cd libpng-1.2.5 cp scripts/makefile.linux makefile make && make install * 다시 php 디렉토리로 와서 ./config.nice 하면 정상 완료가 되어야 합니다. | 64bit linux를 설치하셨고, **install /usr/local/lib/libjpeg.a: could not read symbols: Bad value**에러가 발생했다면, ln -s /usr/lib64/libjpeg.a /usr/lib/libjpeg.a make clean make & make install 위의 과정을 거쳐도 안되면 다음과 같은 작업을 해줍니다. php 디렉토리 안에서, rm ./configure (최근에 configure 안했으면 없을수도 있음) rm auto4te.cache/* ./buildconf --force make clean 가끔 cache 때문에 config.m4의 설정이 configure파일에 반영이 되지 않은 경우가 있습니다. "grep sn2f configure" 해서 아무 결과가 나오지 않으면 sn2f관련 설정이 반영이 되지 않은 경우 입니다. 이때는 "rm autom4te.cache/*" 명령을 주고 다시 ./buildconf [--force]를 해봅니다. |
| --- | --- | --- | --- |
| # # Created by configure './configure' \ '--prefix=/home/fstory/php' \ '--with-config-file-path=/home/fstory/apache/conf' \ '--with-mysql=/home/fstory/mysql' \ '--with-mysqli=/home/fstory/mysql/bin/mysql_config' \ '--with-gd-dir=/home/fstory/gd' \ '--with-jpeg-dir=/usr/lib' \ '--with-apxs2=/home/fstory/apache/bin/apxs' \ '--enable-track-vars' \ '--with-jpeg' \ '--with-iconv' \ '--enable-ftp' \ '--enable-exif' \ '--enable-sockets' \ '--with-xml' \ '--with-gd' \ '--disable-cgi' \ '--with-gif' \ '--with-png' \ '--disable-posix-threads' \ "$@" |  |  |  |
| * 이때 **configure: error: libjpeg.(a|so) not found. **란 오류 메시지가 뜬다면 peg관련해서 하나 더 깔아야 한다. wget [http://freshmeat.net/redir/libjpeg/5665/url_tgz/jpegsrc.v6b.tar.gz](http://freshmeat.net/redir/libjpeg/5665/url_tgz/jpegsrc.v6b.tar.gz) gunzip jpegsrc.v6b.tar.gztar xvf jpegsrc.v6b.tarcd jpeg-6b ./configure make make install (이때 마지막에 에러뜸, 무시해도 좋다. root로 해도 마찬가지임) make install-lib make install-headers. * 다시 php 디렉토리로 와서 ./config.nice그래도 또 이번엔 libpng 가 없다고 나온다면.. ㅜㅜ (아~ 리눅스가 싫다.) wget [http://ftp.superuser.co.kr/pub/etc/libpng-1.2.5.tar.gz](http://ftp.superuser.co.kr/pub/etc/libpng-1.2.5.tar.gz) tar xvfz libpng-1.2.5.tar.gz cd libpng-1.2.5 cp scripts/makefile.linux makefile make && make install * 다시 php 디렉토리로 와서 ./config.nice 하면 정상 완료가 되어야 합니다. |  |  |  |
| 64bit linux를 설치하셨고, **install /usr/local/lib/libjpeg.a: could not read symbols: Bad value**에러가 발생했다면, ln -s /usr/lib64/libjpeg.a /usr/lib/libjpeg.a make clean make & make install 위의 과정을 거쳐도 안되면 다음과 같은 작업을 해줍니다. php 디렉토리 안에서, rm ./configure (최근에 configure 안했으면 없을수도 있음) rm auto4te.cache/* ./buildconf --force make clean 가끔 cache 때문에 config.m4의 설정이 configure파일에 반영이 되지 않은 경우가 있습니다. "grep sn2f configure" 해서 아무 결과가 나오지 않으면 sn2f관련 설정이 반영이 되지 않은 경우 입니다. 이때는 "rm autom4te.cache/*" 명령을 주고 다시 ./buildconf [--force]를 해봅니다. |  |  |  |

<!-- en -->
**1. Title: Super Simple Apache, PHP Installation Manual on Linux**
 * Note: It might not be super simple. It's relatively super simple.
 
**2. Purpose**
I needed to install APM and PHP for work at the company, so I decided to write a manual.
 
**3. Author and Contributors**
**Author: 숲속얘기 [[fstory97@naver.com](mailto:fstory97@naver.com) / [http://blog.naver.com/fstory97](http://blog.naver.com/fstory97)]**
**Contributor: 하얀별님 [[wono77@naver.com](mailto:wono77@naver.com) / [http://blog.naver.com/wono77](http://blog.naver.com/wono77)]**
**The initial draft was written by 하얀별님. Please direct questions to 하얀별님 if possible....**
 
**4. Author's Musings**
How great would it be if there were something like InstallShield, like on Windows ㅡㅡ
There are cases where characteristics vary by version and OS. That's probably why Linux users use the term "distribution" instead of "installation." It's like saying, "It's a hassle, so figure it out and install it yourself." Seeing how various programs have dependencies, it's truly an uphill battle just to use one thing. (I might get flak from Linux users, so no more criticism... It's free, so I should bear with it.)
 
**5. Programs to Install**
* Apache: httpd-2.2.9.tar.gz source
* PHP: 5.2.6 source (stable/alpha is 5.3)
* MySQL: 5.0.67 source (5.1.7 in digitizing/current latest version is 6.0)
 
**6. Environment Tested**
Free 64bit CentOS (Skipping version check as it's bothersome.)
 
**7. Installation Test Date**
October 2, 2008
 
**8. Colors in the Document**
Red indicates parts that the user needs to fill in directly. You can simply Copy & Paste and run the parts written in red.
**** 
**9. Program Download**
You can type the following in the terminal window. (It's fine to Copy & Paste and execute, as long as the links are live..)

 
**10. Decompressing**

 
**11. Apache Installation**

| **1. Create and Run the Batch Shell** Go to the unzipped Apache folder and create an installation shell (executable text file) like the one below. cd httpd-2.2.9/ vi config.nice Grant execute permissions to the .txt file to turn it into a shell script. chmod 755 config.nice Execute it. ./config.nice **** **2. Create Binary Code** make && make install **** **3. Set Account Permissions for the Installation Folder** Move to the installed Apache folder and set account permissions for the folder. You just need to enter your group and name. The installation path is the one specified in --prefix in the shell script. chown -R [group_name]:[account_name] [Installation Path] ex) chown -R fstory:fstory /home/fstory/apahce/ **4. Modify ./conf/httpd.conf** Open and modify the ./conf/httpd.conf file in the installation folder using an editor like vi. *** Green text is explanatory, do not paste it. **** 1) Environment Settings ** Use the Find function in the editor to locate **Listen 80**, then paste the additional options (in red) below it. For 1.x versions, it's Port 80. ** 2) Log Settings ** Settings to store logs in a specific format (myformat) in a different location (/home/logs/https-digit) instead of the default folder when an error occurs. Find ** ErrorLog "logs/error_log"** and change it as shown below. Also, create the /home/logs/https-digit/ folder in advance where logs will be stored. **3) Change Language Type** ** 4) Change Service Default Source Location** Find DocumentRoot and change it to the desired directory (where index.html/ index.php will be located). ** 5) Access Permission Settings (from 2.2.x versions onwards)** Find <Directory />...</Directory> and change it as shown below. **5. Write a Shell Script to Periodically Compress, Backup, and Delete Error Logs as .gz** If it's for an actual service, logs accumulate enormously, so they might need to be periodically deleted. ** 1) Write rotate_log.sh shell script** ** 2) Grant execute permissions by `chmod +x ./rotate_log.sh` **** 3) Apply the shell script to root's crontab** crontab -e **6. Let's Execute It** Go to the Apache installation folder.. ./bin/apachectl start If the log folder has not been created, an error will appear. Create the folder and try running it again. **7. Let's Stop It **./bin/apachectl stop **8. Let's Automatically Start Apache on Server Boot.** S means automatic execution when the server starts, and K means automatic execution when the server shuts down. The number after it is the priority. It will not execute with lowercase 's' or other alphabets. You can execute the commands below by going to the Apache installation bin folder. ******** | #!/bin/sh # # Created by configure "./configure" \ "--prefix=/homel/fstory/apache" \ # Installation path "--enable-so" \ "--enable-rule=SHARED_CORE" \ "--enable-mods-shared=all" \ "--enable-modules=most" \ "--enable-suexec" \ "--with-suexec-caller=bin" \ "--enable-cache" \ "--enable-ext-filter" \ "--with-z=/usr/include" \ "--enable-dav" \ "$@" | Listen 80 # Service port number, default is 80. Timeout 120 # This is the timeout for unresponsive sessions. Our project's is a bit long. KeepAlive off # I don't know if this means it stays alive even without users. Someone tell me.. User fstory # Username Group myteam # Group name DirectoryIndex index.html index.htm index.php # Service starting file name | ErrorLog /home/fstory/logs/https-digit/error # Log location LogFormat "%a - %u %t \"%r\" %s %b - -" myformat SetEnvIf Request_URI "^\/bin\/" dontlog SetEnvIf Request_URI "\.(gif|jpg|png|bmp|swf)" dontlog SetEnvIf Request_URI "\.(css|js)" dontlog CustomLog /home/fstory/logs/https-digit/access myformat env=!dontlog | AddDefaultCharset utf-8 # If this content is missing, the default value operates as euc_kr | DocumentRoot "/home/www_src/" | <Directory /aaa> Order allow,deny allow from all </Directory> | #!/bin/sh LOG_DIR=/home/logs/https-digit DATE=`/bin/date +%y%m%d` YDATE=`/bin/date --date "yesterday" +%Y%m%d` # If cronolog is not used /bin/cp $LOG_DIR/access $LOG_DIR/access.$DATE /bin/cat /dev/null > $LOG_DIR/access /bin/gzip -f $LOG_DIR/access.$DATE # If cronolog is used #/bin/mv $LOG_DIR/access.$YDATE $LOG_DIR/access.$DATE #/bin/gzip $LOG_DIR/access.$DATE #cat /dev/null > $LOG_DIR/error find $LOG_DIR/ -mtime +15 -name "access*" -exec rm {} \; | 01 00 * * * /home/fstory/shell/rotate_log.sh # Specify shell script location | cp apachectl /etc/rc.d/init.d/httpd ln -s /etc/rc.d/init.d/httpd /etc/rc.d/rc3.d/S89httpd ln -s /etc/rc.d/init.d/httpd /etc/rc.d/rc3.d/K89httpd |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| #!/bin/sh # # Created by configure "./configure" \ "--prefix=/homel/fstory/apache" \ # Installation path "--enable-so" \ "--enable-rule=SHARED_CORE" \ "--enable-mods-shared=all" \ "--enable-modules=most" \ "--enable-suexec" \ "--with-suexec-caller=bin" \ "--enable-cache" \ "--enable-ext-filter" \ "--with-z=/usr/include" \ "--enable-dav" \ "$@" | | | | | | | | | |
| Listen 80 # Service port number, default is 80. Timeout 120 # This is the timeout for unresponsive sessions. Our project's is a bit long. KeepAlive off # I don't know if this means it stays alive even without users. Someone tell me.. User fstory # Username Group myteam # Group name DirectoryIndex index.html index.htm index.php # Service starting file name | | | | | | | | | |
| ErrorLog /home/fstory/logs/https-digit/error # Log location LogFormat "%a - %u %t \"%r\" %s %b - -" myformat SetEnvIf Request_URI "^\/bin\/" dontlog SetEnvIf Request_URI "\.(gif|jpg|png|bmp|swf)" dontlog SetEnvIf Request_URI "\.(css|js)" dontlog CustomLog /home/fstory/logs/https-digit/access myformat env=!dontlog | | | | | | | | | |
| AddDefaultCharset utf-8 # If this content is missing, the default value operates as euc_kr | | | | | | | | | |
| DocumentRoot "/home/www_src/" | | | | | | | | | |
| <Directory /aaa> Order allow,deny allow from all </Directory> | | | | | | | | | |
| #!/bin/sh LOG_DIR=/home/logs/https-digit DATE=`/bin/date +%y%m%d` YDATE=`/bin/date --date "yesterday" +%Y%m%d` # If cronolog is not used /bin/cp $LOG_DIR/access $LOG_DIR/access.$DATE /bin/cat /dev/null > $LOG_DIR/access /bin/gzip -f $LOG_DIR/access.$DATE # If cronolog is used #/bin/mv $LOG_DIR/access.$YDATE $LOG_DIR/access.$DATE #/bin/gzip $LOG_DIR/access.$DATE #cat /dev/null > $LOG_DIR/error find $LOG_DIR/ -mtime +15 -name "access*" -exec rm {} \; | | | | | | | | | |
| 01 00 * * | | | | | | | | | |

| /home/fstory/shell/rotate_log.sh # Specify the shell script location | | | | | | | | | |
| cp apachectl /etc/rc.d/init.d/httpd ln -s /etc/rc.d/init.d/httpd /etc/rc.d/rc3.d/S89httpd ln -s /etc/rc.d/init.d/httpd /etc/rc.d/rc3.d/K89httpd | | | | | | | | | |

**
 
**12. MySql Installation**: Based on the 5.0.67 source version.

| **** **1. Create Installation Option Script** vi config.nice (Create it in the directory where you want to install.) **2. Grant Execution Permissions** chmod +x config.nice **3. Execute** ./config.nice **4. Install: Install with root privileges.** make&make install **5. Configuration** Go to the installed mysql directory. vi my.cnf **6. Create Temporary Folder** mkdir tmp **7. Install DB** Must be root privileges. After executing ../bin/mysql_install_db, check if the ./mysql/var directory and its contents have been created. **8. Grant Account Permissions to Folder** Change all permissions of files within mysql to the permissions of the corresponding user ID. chown -R [Group Name]:[Account Name] [Installation Path] **8. Start MySQL.** ./bin/mysqld_safe & If it doesn't start, it might be because /etc/my.cnf already exists. Overwrite with the newly created my.cnf, delete the created db in the /var/lib/mysql folder, and try again from mysql_install_db. **9. Try to shut down MySQL.** ./bin/mysqladmin shutdown **10. Start MySQL again and set the root password.** ./bin/mysqladmin -uroot password '[User Password]' **11. Let's try connecting.** . ./bin/mysql -uroot -p[password] **12. Security Settings (after connecting to MySQL)** use mysql delete from user where user=' '; flush privileges; - Change user password - update user set password=password('root_password') where user='root'; **13. Remote Access Settings for Other Servers (Skip if not needed.)** use mysql insert into user (Host,User,Password) VALUES ('remote_access_ip','account_name',password('password')); **14. Configure MySQL Auto-Start** For installed versions, mysql.server is located at ./mysql/share/mysql. For compiled versions, mysql.server is automatically set up. vi mysql.server cp mysql.server /etc/rc.d/init.d/ ln -s /etc/rc.d/init.d/mysql.server /etc/rc.d/rc3.d/S98mysql******** | ./configure --prefix=/home/fstory/mysql \ #installation path --with-mysqld-user=fstory \ #account name --without-debug \ --with-extra-charsets=euckr --with-charset=utf8 \ --with-unix-socket-path=/home/mysql.sock \ --localstatedir=/home/mysql/var \ --sysconfdir=/home/mysql \ --with-tcp-port=3306 \ #mysql port number --with-readline \ --with-partition \ --with-big-tables \ | [mysql.server] port=3306 socket = /home/fstory/mysql/tmp/mysql.sock #match with installation path datadir = /home/fstory/mysql/var #match with installation path [mysqld] port=3306 socket = /home/fstory/mysql/tmp/mysql.sock #match with installation path datadir = /home/fstory/mysql/var #match with installation path set-variable = max_connect_errors=99999999 set-variable = flush_time=7200 set-variable = max_connection=256 set-variable = table_cache=2000 set-variable = key_buffer=196M set-variable = sort_buffer=4M set-variable = record_buffer=32M set-variable = wait_timeout=20 set-variable = thread_cache_size=60 set-variable = read_buffer_size=32M | basedir=/home/fstory/mysql #Please adjust to the installation folder. datadir=/home/fstoryl/mysql/data # The following variables are only set for letting mysql.server find things.# Set some defaults pid_file=/home/fstory/mysql/tmp/mysql.sock |
| --- | --- | --- | --- |
| ./configure --prefix=/home/fstory/mysql \ #installation path --with-mysqld-user=fstory \ #account name --without-debug \ --with-extra-charsets=euckr --with-charset=utf8 \ --with-unix-socket-path=/home/mysql.sock \ --localstatedir=/home/mysql/var \ --sysconfdir=/home/mysql \ --with-tcp-port=3306 \ #mysql port number --with-readline \ --with-partition \ --with-big-tables \ |  |  |  |
| [mysql.server] port=3306 socket = /home/fstory/mysql/tmp/mysql.sock #match with installation path datadir = /home/fstory/mysql/var #match with installation path [mysqld] port=3306 socket = /home/fstory/mysql/tmp/mysql.sock #match with installation path datadir = /home/fstory/mysql/var #match with installation path set-variable = max_connect_errors=99999999 set-variable = flush_time=7200 set-variable = max_connection=256 set-variable = table_cache=2000 set-variable = key_buffer=196M set-variable = sort_buffer=4M set-variable = record_buffer=32M set-variable = wait_timeout=20 set-variable = thread_cache_size=60 set-variable = read_buffer_size=32M |  |  |  |
| basedir=/home/fstory/mysql #Please adjust to the installation folder. datadir=/home/fstoryl/mysql/data # The following variables are only set for letting mysql.server find things.# Set some defaults pid_file=/home/fstory/mysql/tmp/mysql.sock |  |  |  |

 
**13. PHP Installation**

| **1. Install Sub-Libraries**
**1) Install zlib.... (Must be root)**
./configure --prefix=/home/fstory/zlib
make && make install
**2) Install libiconv... (Install IConv: for euckr->utf8 conversion)**
./configure --prefix=/home/fstoryl/iconv
make && make install
**3) Install libxml2...**
(Failed to install on 64-bit CentOS. ㅜㅜ.)
./configure --prefix=/home/fstory/libxml \
--with-zlib=/home/fstory/zlib \
--with-iconv=/home/fstory//iconv
make && make install
**4) Install libxslt**
(If libxml is not properly linked, this is meaningless. Let's not install it.)
./configure --prefix=/home/fstory/libxslt \
--with-libxml-prefix=/home/fstory/libxml \
--with-libxml-include-prefix=/home/fstory/libxml/include \
--with-libxml-libs-prefix=/home/fstory/libxml/lib
make && make install
**7) Install gd** (Used for thumbnail generation, etc.)
./configure --prefix=/home/fstory/gd \
--with-gif --with-png --with-jpeg
make
make install
**2. Install PHP**
**1) Create and run installation option file**
Please adjust the paths for each program. If something is not installed, remove it.
e.g., If iconv is not installed, remove the --with-iconv line.
vi config.nice
chmod +x config.nice
./config.nice
**2) Exception Handling**
**3) Install**
make & make install
**4) Operation Test and Verification**
Restart Apache and check if it's installed correctly with phpinfo()...
If you commented out AddType or LoadModule libphp5.so for Apache operation testing earlier, uncomment them and restart.
AddType application/x-httpd-php .php4 .php3 .phtml .php .inc .htm .html .naver .nhn
AddType application/x-httpd-php-source .phps
LoadModule php5_module modules/libphp5.so
The settings to change are roughly as follows. (You can just take the previous php.ini and put it in the apache conf directory. Let's put Apache and PHP configuration in the apache conf directory~)
display_errors = On
include_path = ".:/home/fstory/php_includes/common/:/home/fstory/php_includes/"
Write the absolute path of mysql.socket.
mysql.default_socket = /fstory/mysql/tmp/mysql.sock
Check the following in php_info():
Are mysql, mysqli loaded?
Is gd loaded? Especially, is jpg included?
Check iconv
Put the attached php.ini file in the same folder as apache's conf. | # # Created by configure './configure' \ '--prefix=/home/fstory/php' \ '--with-config-file-path=/home/fstory/apache/conf' \ '--with-mysql=/home/fstory/mysql' \ '--with-mysqli=/home/fstory/mysql/bin/mysql_config' \ '--with-gd-dir=/home/fstory/gd' \ '--with-jpeg-dir=/usr/lib' \ '--with-apxs2=/home/fstory/apache/bin/apxs' \ '--enable-track-vars' \ '--with-jpeg' \ '--with-iconv' \ '--enable-ftp' \ '--enable-exif' \ '--enable-sockets' \ '--with-xml' \ '--with-gd' \ '--disable-cgi' \ '--with-gif' \ '--with-png' \ '--disable-posix-threads' \ "$@" | * If you get the error message **configure: error: libjpeg.(a|so) not found.**, you need to install one more jpeg-related package.
wget [http://freshmeat.net/redir/libjpeg/5665/url_tgz/jpegsrc.v6b.tar.gz](http://freshmeat.net/redir/libjpeg/5665/url_tgz/jpegsrc.v6b.tar.gz)
gunzip jpegsrc.v6b.tar.gz
tar xvf jpegsrc.v6b.tar
cd jpeg-6b
./configure
make
make install (An error will appear at the end, but you can ignore it. It's the same even as root.)
make install-lib
make install-headers.
* Come back to the php directory and run ./config.nice. If libpng is still missing... ㅜㅜ (Ugh, I hate Linux.)
wget [http://ftp.superuser.co.kr/pub/etc/libpng-1.2.5.tar.gz](http://ftp.superuser.co.kr/pub/etc/libpng-1.2.5.tar.gz)
tar xvfz libpng-1.2.5.tar.gz
cd libpng-1.2.5
cp scripts/makefile.linux makefile
make && make install
* Come back to the php directory and run ./config.nice. It should complete successfully. | If you installed 64bit Linux and encountered the error **install /usr/local/lib/libjpeg.a: could not read symbols: Bad value**, then:
ln -s /usr/lib64/libjpeg.a /usr/lib/libjpeg.a
make clean
make & make install
If the above steps don't work, perform the following actions:
Inside the php directory:
rm ./configure (May not exist if configure hasn't been run recently)
rm auto4te.cache/*
./buildconf --force
make clean
Sometimes, due to cache issues, the settings in config.m4 are not reflected in the configure file. If "grep sn2f configure" yields no results, it means the sn2f-related settings were not applied. In this case, run "rm autom4te.cache/*" and then try ./buildconf [--force] again. |
| --- | --- | --- | --- |
| # # Created by configure './configure' \ '--prefix=/home/fstory/php' \ '--with-config-file-path=/home/fstory/apache/conf' \ '--with-mysql=/home/fstory/mysql' \ '--with-mysqli=/home/fstory/mysql/bin/mysql_config' \ '--with-gd-dir=/home/fstory/gd' \ '--with-jpeg-dir=/usr/lib' \ '--with-apxs2=/home/fstory/apache/bin/apxs' \ '--enable-track-vars' \ '--with-jpeg' \ '--with-iconv' \ '--enable-ftp' \ '--enable-exif' \ '--enable-sockets' \ '--with-xml' \ '--with-gd' \ '--disable-cgi' \ '--with-gif' \ '--with-png' \ '--disable-posix-threads' \ "$@" |  |  |  |
| * If you get the error message **configure: error: libjpeg.(a|so) not found.**, you need to install one more jpeg-related package.
wget [http://freshmeat.net/redir/libjpeg/5665/url_tgz/jpegsrc.v6b.tar.gz](http://freshmeat.net/redir/libjpeg/5665/url_tgz/jpegsrc.v6b.tar.gz)
gunzip jpegsrc.v6b.tar.gz
tar xvf jpegsrc.v6b.tar
cd jpeg-6b
./configure
make
make install (An error will appear at the end, but you can ignore it. It's the same even as root.)
make install-lib
make install-headers.
* Come back to the php directory and run ./config.nice. If libpng is still missing... ㅜㅜ (Ugh, I hate Linux.)
wget [http://ftp.superuser.co.kr/pub/etc/libpng-1.2.5.tar.gz](http://ftp.superuser.co.kr/pub/etc/libpng-1.2.5.tar.gz)
tar xvfz libpng-1.2.5.tar.gz
cd libpng-1.2.5
cp scripts/makefile.linux makefile
make && make install
* Come back to the php directory and run ./config.nice. It should complete successfully. |  |  |  |
| If you installed 64bit Linux and encountered the error **install /usr/local/lib/libjpeg.a: could not rea |

d symbols: Bad value** If an error occurs,
`ln -s /usr/lib64/libjpeg.a /usr/lib/libjpeg.a make clean make & make install`
If the above steps don't work, perform the following tasks:
Inside the php directory,
`rm ./configure` (It might not exist if you haven't run configure recently)
`rm auto4te.cache/*`
`./buildconf --force`
`make clean`
Sometimes, due to cache issues, the settings in `config.m4` might not be reflected in the `configure` file. If "grep sn2f configure" yields no results, it means the sn2f-related settings have not been applied. In this case, try running the "rm autom4te.cache/*" command and then `./buildconf [--force]` again. | | | |