# Fstory 옛 홈페이지 복원 — 완료 보고

이 문서는 이전 핸드오프를 대체합니다. 목표로 잡았던 작업은 끝났고, 아래는 무엇을 했고 무엇이 끝내 없었는지를 정직하게 나눈 기록입니다.

사람용 설명은 `public/fstory-homepage/README.md` 에 따로 있습니다.

## 결론부터

`www.fstory.net` 의 인터넷 아카이브 보관본에서 회수할 수 있는 범위를 다 회수했습니다. 캡처 9개를 게시했고, 브라우저로 도달할 수 있는 모든 페이지를 실제로 열어 확인했으며, 죽은 링크와 깨진 그림은 남아 있지 않습니다.

여기에 카페루아가 따로 보관하던 1997년 판과 1998~2001.07 판도 같은 기준으로 손봤습니다. 아틀리에가 같은 바탕화면에서 함께 열어 주기 때문입니다.

## 무엇이 정해졌나

### 디자인 계보는 여섯 세대다

캡처 9개는 여섯 세대로 갈립니다. 판단 근거는 각 캡처의 `index.html` 이 불러오는 프레임 사슬 전체와, 메뉴 링크 집합과, DOM 골격 해시입니다. 세 가지가 모두 같아야 같은 세대로 봅니다.

L0 은 2001-07-15 도메인 주차 화면이고 홈페이지 본체가 아닙니다. L1 은 1998년부터 2001-07-23 까지의 클래식 계보로, 프레임 없이 `title1~4.gif` 메뉴 그림을 늘어놓은 형태입니다. L2.0 은 2001-09 부터 2002-03 까지, L2.1 은 2002-03 부터 2002-11-20 까지, L2.2 는 2002-11-28 부터, L3 은 2003-07-26 마지막 판입니다.

계보 정의는 `scripts/fstory-lineage.mjs` 에 있고, 그 근거를 다시 계산하는 도구는 `scripts/analyze-fstory-lineage.py` 입니다.

### 2002-11-20 과 11-28 은 합치지 않았다

루크가 다르다고 한 기억이 맞았습니다. 8일 차이인데 `main.html` 이 완전히 다릅니다. 11-20 은 `frameset rows="42,560"` 두 칸이고, 11-28 은 `rows="50,515,15"` 안에 `cols="5,420,5"` 를 중첩한 5분할에 BGM 프레임이 붙습니다. 11-28 에서 album·cartoon·media·insidece 메뉴가 새로 생기고 싸이월드 링크가 처음 등장합니다.

별개 리비전으로 남겼고, `tests/legacyHomepages.test.ts` 가 이 구분을 지킵니다.

반대로 2002-09-24 와 11-20 은 배경 그림 하나(`back.gif` ↔ `back.jpg`)만 다른 같은 판입니다. 파일 수가 캡처마다 다른 것도 대부분 사이트가 커진 게 아니라 웨이백이 그 기간에 얼마나 많이 훑었는지의 차이입니다.

## 숫자

게시 파일 5,324개. 보관본을 뒤져 되살린 파일 791개. 다시 쓴 참조 1,062개.

옛 주소까지 훑기 전에는 3,373개였습니다. 네티앙 시절 223건과 천리안 41건, 카페24 6건이 더해지면서 늘었습니다.

없는 자리 처리는 이렇습니다. 그림 자리표시자 6,665개, 배경 속성 제거 3,732개, 안내 페이지 연결 10,815개, 음악·영상 링크 해제 1,309개, 첨부 내려받기 해제 20개, 폼 비활성 472개. 싸이월드로 넘기던 루크 본인 링크 17개 파일은 전용 안내 페이지로 연결했습니다.

누락 참조는 처음 9,429건에서 43건으로 줄었습니다. 남은 43건은 원본의 깨진 `javascript:void(window.open("'…'"` 마크업을 감사기가 다시 읽으면서 생기는 것이고, 실제 출력은 이미 안내 페이지를 가리킵니다.

판별 파일 수는 2001-07-15 판 243개에서 2003-07-26 판 905개까지입니다. 1997년 판은 253개 파일 가운데 7개 경로만 끝내 없고, 1998~2001.07 판은 미복원 0입니다.

## 1997년 판의 시 27편을 되살렸다

루크가 사이트를 다시 만들 때마다 자기 시를 그대로 옮겨 실었기 때문에, 1997년 판이 목록에만 남기고 본문을 잃은 시가 2001~2003년 판의 `myletter/poem/` 아래에 그대로 남아 있었습니다. 33편 가운데 27편을 되살렸습니다.

짝을 맞춘 근거는 세 가지입니다. 목록이 적어 둔 날짜와 아카이브 글의 날짜가 같은가, 파일 이름이 제목과 같은가, 그리고 목록의 제목이 본문 첫 줄인가입니다. 1997년 판이 제목 자리에 그 시의 첫 행을 쓴 경우가 많아 마지막 근거가 특히 잘 듣습니다. 「쓸쓸한 저녁놀..」은 `p헤어짐.html` 의 첫 줄이 그대로 "쓸쓸한 저녁놀 흐르는 긴 그림자 뒤로 밟는 나의 마음엔" 입니다.

추측을 배제하려고 **답을 아는 곳에서 먼저 맞춰 봤습니다.** 본문이 남아 있는 42편에 같은 매칭기를 돌려 36편을 맞혔고(86%), 그 정확도를 내는 증거 조합만 채택 기준으로 삼았습니다. 후보가 둘 이상이면서 점수가 같으면 채택하지 않습니다. 도구는 `scripts/restore-1997-poems.py` 이고, `--apply` 없이 실행하면 무엇을 왜 채택하는지만 보여 줍니다.

끝내 못 찾은 6편은 1996년 4월에서 6월 사이에 몰려 있습니다. 「나는 열정이..」, 「사랑하는 사람을 잃으신다면.」, 「정 직」, 「당신에게 내모습자신없어..」, 「웃음과 눈물의 의미」, 「당신께서 세상을..」 입니다.

네티앙 시절까지 받아 시가 162편으로 늘어난 뒤 다시 찾아봤지만 여섯 편은 여전히 없었습니다. 네티앙의 1996년 목록(`netian/mydoc/poem/95_96/poem96.html`)은 「세상을 적시는」 부터 시작하는데, 사라진 여섯 편은 그보다 앞입니다. 제목 문구로 복구 자료 10,887개 파일을 전수 검색해도 나오지 않습니다. 다른 글에 우연히 겹친 낱말만 걸립니다. 다음 판으로 옮기지 않은 것으로 보입니다.

시 매칭 정확도는 네티앙 자료가 들어오면서 86%에서 91%로 올랐습니다. 이제 본문이 남아 있는 69편 가운데 63편을 맞힙니다.

## 방명록은 모든 캡처를 합쳐 다시 세웠다

천리안 방명록은 쪽 단위로 캡처돼서, 어떤 글은 이 캡처에만 어떤 글은 저 캡처에만 있습니다. 한 캡처만 쓰면 나머지가 사라집니다. 그래서 캡처 30개에 흩어진 글을 번호로 합쳐 한 페이지로 다시 세웠습니다.

글 91편이 모였습니다. 1번 「드뎌 방명록 완성 !」(2001-09-21)부터 240번(2002-10-26)까지입니다. 그 사이 149개 번호는 애초에 캡처되지 않았고, 페이지에도 그렇게 적어 두었습니다.

같은 글이 여러 캡처에 있으면 가장 긴 판을 씁니다. 나중 캡처에는 답글이 더 달려 있을 수 있기 때문입니다. 도구는 `scripts/merge-fstory-guestbook.mjs` 이고 결과와 근거는 `../data/fstory-net-wayback/versions/merged/` 에 남습니다.

## 게시판 페이지를 전부 정적 HTML로 바꿨다

복구된 게시판 페이지 353개가 `.php` 와 `.cgi` 라는 이름을 달고 있었습니다. 내용은 이미 정적 HTML인데 이름만 서버 프로그램 것이었습니다. 이대로면 정적 호스팅이 내려받기로 처리하거나 아예 거부합니다.

전부 `.html` 로 바꾸고 그 파일을 가리키던 링크도 함께 고쳤습니다. 원래 주소는 아카이브 목록과 복구 보고서에 남습니다. 이 변환 덕분에 `next.config.mjs` 에 넣었던 예외 규칙도 걷어냈습니다. 이제 어떤 정적 호스팅에서도 그대로 열립니다.

## 없는 것을 어떻게 다뤘나

원자료가 없는 자리를 채우지 않았습니다. 대신 무엇이 없는지 보이게 했습니다.

그림이 없으면 「그림 없음」 자리표시자가 들어가고 원래 주소는 `data-unrestored-src` 에 남습니다. 페이지가 없으면 그 시대 양식의 안내 페이지로 가고, 안내 페이지가 찾던 주소와 없는 이유를 보여 줍니다. 배경 그림이 없으면 속성을 떼어 브라우저가 헛되이 요청하지 않게 했습니다. 음악·영상과 첨부 내려받기는 링크 자체를 없앴습니다. 글쓰기·검색·로그인 폼은 전부 막았습니다.

주소가 정확히 같을 때만 다른 시점 보관본에서 파일을 가져왔습니다. 이름만 같은 것은 쓰지 않았습니다. 제로보드 스킨은 색깔만 다른 폴더에 같은 이름의 그림이 잔뜩 있어서, 이름만 맞춰 가져오면 다른 색이 섞입니다. 예외는 `mydoc/novel/` → `myletter/` 하나이고, 사이트가 실제로 폴더를 옮긴 것이며 같은 파일이 양쪽에서 32쌍 확인됩니다.

## 이 홈페이지가 살았던 주소들

한 사이트인데 주소가 여러 개였습니다. 전부 훑어서 하나로 모았습니다.

`fstory.net` 과 `www.fstory.net` 이 도메인이고, `fstory.cafe24.com` 은 그 도메인이 가리키던 호스팅 계정입니다. 그 이전에는 `my.netian.com/~fstory` 에 있었고, 배경음악과 방명록은 `user.chollian.net/~foreststory` 와 `cgi.chollian.net/~foreststory` 가 맡았습니다.

같은 서버를 여러 사람이 나눠 쓰던 시절이라 이름만으로는 갈리지 않습니다. `user.chollian.net/~ahnjiy/` 는 남의 계정입니다. 그래서 수집기와 게시 스크립트 모두 호스트와 경로를 함께 봅니다.

각 시대는 자기 하위 폴더로 들어옵니다. `chollian/`, `chollian/cgi/`, `netian/` 입니다. 시대마다 같은 이름의 다른 파일이 있어서 한 곳에 합치면 서로 덮어씁니다.

`fstory.com.ne.kr` 의 기독교 코너는 아카이브에 한 건도 없습니다. 회수할 방법이 없습니다.

## 옛 주소는 아틀리에에서 따로 고를 수 있다

문제가 하나 있었습니다. 천리안 자료는 ver 2.0 판의 메뉴와 배경음악 프레임이 실제로 가리키던 것이라 저절로 닿습니다. 그런데 네티앙 시절은 도메인을 쓰기 전이라, 이후 페이지가 거슬러 가리킬 이유가 없습니다. 복구는 됐는데 아무도 못 보는 상태가 됩니다.

없던 링크를 옛 페이지에 새로 심는 것은 원형 복원이 아닙니다. 대신 아틀리에의 복원 시점 선택 목록에 「fstory.net 이전 주소」 라는 묶음을 두고 거기서 고르게 했습니다. 옛 페이지는 그대로 두고, 우리 UI 쪽에서만 길을 냅니다.

네티앙 첫 화면은 `title1~4.gif` 이미지맵 메뉴를 씁니다. L1 클래식 계보와 같은 그림입니다. 그 디자인이 네티앙에서 왔다는 증거입니다. 화면에 "1999.3.17부터 방문자" 와 "100% 메모장 노가다" 라고 적혀 있습니다.

진입 경로는 `scripts/fstory-lineage.mjs` 의 `ANNEXES` 에 있고, 생성 스크립트가 실행할 때마다 그 파일이 실제로 있는지 확인합니다. 없으면 곧바로 실패합니다.

## 천리안 시절 방명록이 통째로 남아 있었다

`cgi.chollian.net/~foreststory/pury/purybbs.cgi` 는 PURY BBS 라는 CGI 방명록이었습니다. 목록 화면에 글 본문이 그대로 실리는 형식이라, 2001년에 오간 글과 답글이 캡처된 24쪽에 통째로 남아 있습니다. 재균아빠, 재현이, 괴인, 졸곰 같은 분들이 남긴 글과 숲속얘기가 단 답글이 그대로입니다.

배경음악 프레임 `chollian/bgm/bgm.html` 도 돌아왔습니다. ver 2.0 판의 위쪽 프레임이 이 페이지를 부르던 것이었고, 그전에는 죽은 바깥 주소였습니다. 음악 파일 자체(`bgm01.wma` 등)는 보관되지 않았습니다.

## 카페24 주소도 같은 사이트다

`www.fstory.net` 은 도메인이고 `fstory.cafe24.com` 은 그 도메인이 가리키던 호스팅 계정입니다. 2003년 판이 게시판 링크를 호스팅 이름으로 썼기 때문에, 두 이름을 한 호스트로 취급하자 「잡담이 섞인 아이스크림」, 「공부벌래 쿠키」, 「문앞에 걸린 조그만 게시판」 세 메뉴가 이미 복구된 제로보드 페이지로 이어졌습니다. 그전에는 죽은 바깥 링크였습니다.

수집기에도 이 이름을 더했습니다. 새로 건진 것은 많지 않습니다. `kani/special/` 아래 애니메이션 감상기 세 편(정글은 언제나 하레나와 구우 OVA, 프린세스 디지, 유시)과 2006년 사진 세 장입니다. 감상기 세 편은 `kani/kani.html` 메뉴가 실제로 가리키던 페이지라 이제 열립니다.

나머지는 사이트가 아니었습니다. 카페24가 빈 주소에 돌려주던 가입 축하 안내(2003-04-01)와 계정이 끝난 뒤의 "연결된 웹사이트 없음" 안내(2019-07-04)입니다. 둘 다 그대로 두면 어느 판의 첫 화면 자리를 차지하므로 수집기에서 지문으로 걸러 냅니다.

## 브라우저 검사가 잡은 것

정적 감사만으로는 안 보이던 결함이 넷 나왔습니다.

첫째, 롤오버 메뉴가 `이미지.src = "…"` 로 미리 불러오던 스킨 그림입니다. 속성이 아니라 스크립트 안에 있어서 어떤 속성 재작성도 닿지 않았습니다.

둘째, 2001-07-15 주차 페이지의 자동 이동입니다. 주소 뒤에 `main.html` 을 붙여 옮겨 가는데 그 페이지는 보관되지 않았습니다.

셋째, 제로보드가 확장자 없이 저장한 아이콘 파일 20개입니다. 확장자가 없으면 지금 호스팅의 `trailingSlash` 설정 때문에 308 로 튕겨 그림이 도착하지 못합니다. 각 파일의 바이트가 말하는 확장자를 붙였습니다.

넷째, 1997년 판이 `space.jpg` 를 요청하는데 실제 파일은 `space.JPG` 인 경우입니다. 당시 윈도우 서버는 대소문자를 가리지 않았지만 지금 호스팅은 가립니다. 같은 계열로 `..\..\common\post2.gif` 같은 역슬래시 경로와, 사이트 뿌리 위로 한 단계 더 올라가는 `../../` 도 함께 바로잡았습니다. 셋을 고치자 1997년 판의 깨진 참조가 40개에서 7개로 줄었습니다.

`.php` 로 끝나는 게시판 페이지가 내려받기로 열리던 것도 함께 고쳤습니다. `next.config.mjs` 에서 그 경로의 내용 유형을 지정합니다.

## 다시 만들고 확인하는 법

```bash
cd projects/cafelua.com/public-home
node scripts/publish-fstory-homepages.mjs
python scripts/analyze-fstory-lineage.py
python scripts/audit-fstory-homepages.py
npx jest tests/legacyHomepages.test.ts
npx playwright test tests/e2e/fstoryRestoredCaptures.spec.ts
npm run build
```

생성 스크립트는 매번 폴더를 지우고 처음부터 다시 만들기 때문에 몇 번을 돌려도 같은 결과가 나옵니다. 1997/1998 판만 예외로 제자리에서 손보는데, 이미 손본 표시를 읽어 두 번 처리하지 않고 보고 숫자도 그대로 유지합니다.

플레이라이트 브라우저가 이 PC에 완전히 설치되어 있지 않아 다음처럼 기존 크로미움을 지정해 돌렸습니다.

```bash
PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/home/luke/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome \
E2E_PORT=3319 npx playwright test tests/e2e
```

## 손대지 말아야 할 것

`public/fstory-homepage` 는 전부 생성물입니다. 고칠 곳은 `scripts/publish-fstory-homepages.mjs` 와 `scripts/fstory-lineage.mjs`, 그리고 원자료인 `../data/fstory-net-wayback/` 입니다.

`public/1997-homepage` 와 `public/1998-homepage` 는 루크가 직접 보관하던 파일입니다. 생성 스크립트가 제자리에서 손보되, 원래 주소는 모두 `data-unrestored-*` 속성이나 안내 페이지의 주소란에 남겨 두었습니다.

## 합치지 않고 남긴 것

같은 세대 안에서도 캡처마다 다른 것이 있습니다. `frame1.html` 위쪽 띠에는 2001년 12월이면 「크리스마스용 임시 대문.. ^^ 메리크리스마스 되세요.」, 2002년 3월이면 「병서기의 홈에 오신것을 환영합니다. 2002년 새해 복많이 받으세요. ^^」 라고 적혀 있습니다. AI 코너와 애니 코너의 소개글도 초기 문구가 나중에 교체됐습니다.

이런 문구는 대표판 것을 씁니다(루크 결정, 2026-08-30). 사라지는 문구는 여기 적어 둡니다.

- `frame1.html` 2001-12-02 판: 「크리스마스용 임시 대문.. ^^ 메리크리스마스 되세요.」
- `frame1.html` 2002-03-25 판: 「병서기의 홈에 오신것을 환영합니다. 2002년 새해 복많이 받으세요. ^^」
- `ai/ai.html` 2001-09-25 판: 「이곳 게시판(우측상단)에서는 여러분의 의견과 기사를 받습니다. 검토후 편집해서 이곳에 올려 드릴게요. 이곳이 AI에 관심있는 분들에게 도움이 될 수 있었으면 좋겠습니다.」
- `kani/kani.html` 2001-09-25 판: 「한국에서 방영되었던 에니메이션 중 개인적으로 좋아하던 한글 에니메이션 음악을 정리할 생각입니다. 신청곡주시면 그것부터 해드릴게여. ^^」

원자료는 그대로 남아 있으니 생각이 바뀌면 되돌릴 수 있습니다.

## 남은 것

1997년 판의 시 6편은 어느 판에도 다시 실리지 않았습니다. 루크가 옛 백업을 가지고 있다면 그 파일들을 `public/1997-homepage/gl/poem/` 에 넣는 것만으로 안내 페이지가 자동으로 실제 페이지로 바뀝니다. 생성 스크립트가 안내 링크에 원래 주소를 기록해 두고, 그 주소에 파일이 생기면 링크를 되돌립니다.

`fstory.cafe24.com` 도 훑었습니다. 카페24는 `www.fstory.net` 이 가리키던 호스팅 계정이라 같은 사이트입니다.

싸이월드 미니홈피는 복구하지 않습니다. 서비스가 종료됐고, 무엇보다 우리 서비스가 아니어서 그 안의 글과 남의 댓글을 우리 쪽으로 옮기지 않기로 했습니다(루크 결정, 2026-08-30). 해당 메뉴는 실제 주소를 밝힌 안내 페이지로 연결합니다.
